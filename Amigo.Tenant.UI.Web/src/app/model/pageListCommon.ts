

export class PageListCommon {
     page: number;
     pageSize: number;

     constructor(page?: number, pageSize?: number) {
        
        this.page = 1;
        this.pageSize = 20;

     }

     
}

